# Empty on purpose. Its presence marks this directory as pytest's rootdir
# for the tests/ package below, so pytest inserts it into sys.path
# automatically (default "prepend" import mode) — no per-test-file
# sys.path.insert(...) needed to import document_validator.
